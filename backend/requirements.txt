# FastAPI and server
# Pinned to the versions this codebase's test suite is verified against. The
# previous pins (fastapi 0.109 / python-multipart 0.0.6 / python-jose 3.3.0 /
# jinja2 3.1.3) each carried published CVEs — see docs/SECURITY_AUDIT.md.
fastapi==0.135.1
uvicorn[standard]==0.41.0
# 0.0.6 was vulnerable to CVE-2024-24762 (ReDoS on Content-Type) and
# CVE-2024-53981 (resource exhaustion on malformed multipart).
python-multipart==0.0.31

# Database
sqlalchemy==2.0.48
asyncpg==0.31.0
alembic==1.18.4

# Redis
redis==7.3.0
aioredis==2.0.1

# Geometry processing
trimesh[easy]==4.11.3
numpy==2.4.3
numpy-stl==3.2.0
cascadio>=0.0.6
# Exact B-rep feature recognition for STEP files (holes, machining
# directions). Optional at runtime — brep.py degrades gracefully without it.
cadquery-ocp>=7.7.0

# PDF generation
# Left untouched: WeasyPrint needs system libraries that are absent on this
# development host, so an upgrade here cannot be verified before shipping.
# 68.0 fixes PYSEC-2026-2034. Verified against the real quote template in the
# Linux container (the only place the system libs exist) before pinning.
weasyprint==68.0
# pydyf 0.10 was pinned for weasyprint 60.x; 68 requires the newer API.
pydyf>=0.11.0
# 3.1.3 was vulnerable to CVE-2024-34064 / CVE-2024-56201 / CVE-2025-27516
# (sandbox escapes). Our templates are trusted and autoescaped, but the floor
# costs nothing.
jinja2==3.1.6
reportlab==4.0.8  # Fallback for Windows (no system dependencies)

# Validation and utilities
pydantic==2.12.5
pydantic-settings==2.14.2
# 3.3.0 was vulnerable to CVE-2024-33663 (algorithm confusion) and
# CVE-2024-33664 (JWE decompression bomb).
python-jose[cryptography]==3.5.0
passlib[bcrypt,argon2]==1.7.4
# Argon2id is the default password hash; bcrypt stays for legacy verification.
argon2-cffi==23.1.0
# Do not move past 4.0.x: passlib 1.7.4 reads bcrypt.__about__, which bcrypt
# 4.1 removed. Upgrading bcrypt means replacing passlib first.
bcrypt==4.0.1

# File handling
aiofiles==25.1.0
boto3==1.43.45
python-magic-bin==0.4.14;sys_platform=='win32'
python-magic==0.4.27;sys_platform!='win32'

# Background tasks
celery==5.3.4
httpx==0.28.1
stripe==15.3.0

# Development
pytest==9.1.1
pytest-asyncio==1.4.0
black==26.3.1
# Dependency vulnerability scanning: python -m pip_audit
pip-audit==2.9.0
